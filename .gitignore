# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Von 'flutter create' erzeugte Plattform-Ordner (CI erzeugt sie neu)
/android/
/ios/
/linux/
/macos/
/windows/
# web/ wird teilweise committed (Manifest + Icons fuer PWA), daher kein globaler Ausschluss
/web/*.html
/web/*.dart
/web/flutter*

# IDE
.idea/
.vscode/
*.iml
